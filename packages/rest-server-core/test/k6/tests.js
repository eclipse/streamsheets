// import http from 'k6/http';

// const host = 'localhost'; // change to actual IP address of host

// export default function() {
// 	for (let id = 1; id <500; id++) {
// 		http.get(`http://${host}:8084/api/v1.0/cedalo/wildcards/tests?json={"request":"${id}","tests":"tests2"}&example=true&example2=true`);
// 	}
// };
